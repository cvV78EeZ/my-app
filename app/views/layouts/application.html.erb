<!DOCTYPE html>
<html lang="ja">
<html>
  <head>
    <title>Eesti推しスポット</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="<%= mix('/css/app.css') %>">
    <script src="<%= mix('/js/bootstrap.js') %>"></script>
  </head>

  <body>
  <div class="main-container">
    <header>
      <div class="logo">
        ひとまず渡航準備
      </div>
      <nav class="custom-navbar">
        <div id="menuToggle">
          <input type="checkbox" />
          <span></span>
          <span></span>
          <span></span>
          <ul id="menu">
            <li>
            <%= link_to "国一覧", toppage_countries_path, class: "nav-link" %>
            </li>
            <li>
            <%= link_to "日本の国一覧", japan_countries_path, class: "nav-link" %>
            </li>
          </ul>
        </div>
      </nav>
    </header>
    <!--  ナビゲーションバーの追加 -->
  

    <!-- アラートメッセージの追加 -->
    <% if notice %>
      <div class="alert alert-success" role="alert">
        <%= notice %>
      </div>
    <% end %>
    <% if alert %>
      <div class="alert alert-danger" role="alert">
        <%= alert %>
      </div>
    <% end %>

    <div class="bg-white text-dark">
      <%= yield %>
    </div>
  </div>

  <!-- Javascript追加 -->
  <script src="<%= mix('/js/app.js') %>"></script>
</body>
</html>
